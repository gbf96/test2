<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
<div class="container">
    <a class="navbar-brand" href="/">FOCCACIA ⚽</a>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
    
    <ul class="navbar-nav me-auto">
        <li class="nav-item">
        <a class="nav-link" href="/groups">My Groups</a>
        </li>
        <li class="nav-item">
        <a class="nav-link" href="/groups/new">+ Create Group</a>
        </li>        
        <li class="nav-item">
        <a class="nav-link" href="/competitions">🏆 View Competitions</a>
        </li>
    </ul>
    
    {{#unless hideNav}}
    <div class="d-flex">
        {{#if user}}
        <span class="navbar-text me-3 text-light">Hello, {{user.username}}</span>
        <form action="/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-danger btn-sm">Logout</button>
        </form>
        {{else}}
        <a href="/login" class="btn btn-primary btn-sm">Login</a>
        {{/if}}
    </div>
    {{/unless}}

    </div>
</div>
</nav>
